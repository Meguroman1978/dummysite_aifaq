# 手動HTMLソースコード入力機能の実装完了

## 📋 実装概要

403や404エラーでウェブサイトの自動取得が失敗する場合に、ユーザーが手動でHTMLソースコードを入力できる代替機能を実装しました。

## ✨ 新機能

### 1. タブシステムの追加
- **URL入力タブ**: 従来通りのURL自動取得
- **ソースコード入力タブ**: 手動でHTMLを貼り付け

### 2. エラーハンドリングの強化
- 403/404エラー発生時に自動的に手動入力オプションを提案
- 視覚的なエラーメッセージボックスを追加
- ソースコードの取得方法を明示的に案内

### 3. 手動HTML処理機能
- 大きなテキストエリアでHTMLコードを受け付け
- オプションでベースURLを指定可能（相対URL解決用）
- 既存の画像プロキシ機能と統合
- 自動取得と同じ処理パイプラインを使用

## 📝 変更ファイル

### 1. `public/index.html`
- タブシステムのUI追加
- 手動HTML入力用のテキストエリア追加
- ベースURL入力フィールド追加
- エラーメッセージ表示エリア追加

### 2. `public/app.js`
- タブ切り替えロジック実装
- `handleUrlInput()` と `handleManualInput()` 関数を分離
- エラー表示機能の強化（`showError()` に手動入力提案オプション追加）
- 手動HTML検証（基本的なHTMLタグ確認）

### 3. `server.js`
- `/api/fetch-website` エンドポイントを拡張
- `manualHtml` パラメータのサポート追加
- `baseUrl` パラメータのサポート追加
- 変数名の競合を修正（`baseUrl` → `resolvedBaseUrl`）
- ベースURLがない場合の適切な処理

### 4. `public/styles.css`
- タブボタンのスタイリング
- テキストエリアのスタイリング
- エラーボックスのアニメーション追加
- レスポンシブデザインの維持

## 🎯 使用方法

### 自動取得が失敗した場合：

1. **ソースコードの取得**
   - 複写したいウェブサイトにアクセス
   - 右クリック → 「ページのソースを表示」
   - すべて選択してコピー（Ctrl+A → Ctrl+C）

2. **手動入力**
   - 「ソースコード入力」タブに切り替え
   - テキストエリアにHTMLを貼り付け
   - （オプション）ベースURLを入力（相対URLを解決するため）
   - AIFAQ URL を入力
   - 「HTMLを処理」ボタンをクリック

3. **その後の流れは同じ**
   - スクリプト埋め込み位置を選択
   - HTMLをダウンロード

## 🔧 技術的な詳細

### エラーハンドリング
```javascript
// 403/404エラー時に手動入力オプションを表示
const shouldShowManualOption = data.code === 'HTTP_403' || data.code === 'HTTP_404';
showError('エラータイトル', 'メッセージ', '詳細', shouldShowManualOption);
```

### 手動HTML処理
```javascript
// サーバーサイド
if (manualHtml) {
  html = manualHtml;
  fetchMethod = 'manual-input';
  // baseUrlが提供されている場合は使用
  if (baseUrl) {
    targetUrl = new URL(baseUrl);
  }
}
```

### URL解決の改善
- `resolvedBaseUrl` を使用して相対URLを絶対URLに変換
- ベースURLがない場合でも既存の `<base>` タグを検出
- プロトコル相対URL（//example.com）のフォールバック処理

## ✅ テスト結果

- サーバーが正常に起動: ✅
- タブ切り替え機能: ✅
- エラーメッセージ表示: ✅
- 手動HTML処理API: ✅

## 🚀 デプロイ状況

- ローカルコミット完了: ✅
- ブランチ: `genspark_ai_developer`
- コミットハッシュ: `83c2f21`
- サーバーURL: https://3000-imuzlcq72rlqaw3utsprj-c81df28e.sandbox.novita.ai

## 📌 次のステップ

リポジトリの所有者が以下を実行する必要があります：

1. ローカル環境で変更を確認
2. `genspark_ai_developer` ブランチをリモートにプッシュ
3. GitHub上でPull Requestを作成
4. PR説明に本ドキュメントの内容を含める

## 🎉 まとめ

この実装により、Bot対策が厳しいウェブサイト（403エラー）やアクセスできないページ（404エラー）でも、ユーザーがソースコードを手動で取得して貼り付けることで、同様にFirework AIFAQウィジェットを埋め込むことができるようになりました。

ユーザーエクスペリエンスも向上し、エラー発生時に明確な代替手段が提示されるようになっています。
